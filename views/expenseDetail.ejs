<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Detail | SpendSync</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-50 min-h-screen p-4 md:p-8">
    <div class="max-w-2xl mx-auto">
        <a href="/groups/<%= groupId %>" class="inline-flex items-center text-purple-600 font-semibold mb-6 hover:underline">
            ← Back to Group
        </a>

        <div class="bg-white rounded-3xl shadow-sm border border-gray-100 overflow-hidden">
            <div class="p-8 bg-purple-600 text-white">
                <p class="text-purple-200 text-xs font-bold uppercase tracking-widest mb-2">Expense Details</p>
                <h1 class="text-3xl font-bold mb-1"><%= expense.description %></h1>
                <p class="text-purple-100"><%= new Date(expense.date).toDateString() %></p>
            </div>

            <div class="p-8">
                <div class="grid grid-cols-2 gap-6 mb-8">
                    <div>
                        <p class="text-gray-400 text-xs font-bold uppercase mb-1">Total Amount</p>
                        <p class="text-3xl font-black text-gray-800">₹<%= expense.totalAmount.toLocaleString('en-IN') %></p>
                    </div>
                    <div>
                        <p class="text-gray-400 text-xs font-bold uppercase mb-1">Paid By</p>
                        <p class="text-xl font-bold text-purple-600"><%= expense.paidBy.username %></p>
                        <p class="text-xs text-gray-500"><%= expense.paidBy.email %></p>
                    </div>
                </div>

                <hr class="border-gray-50 mb-8">

                <h3 class="font-bold text-gray-800 mb-4 flex justify-between">
                    <span>Individual Breakdown</span>
                    <span class="text-xs text-gray-400 bg-gray-100 px-2 py-1 rounded uppercase"><%= expense.splitType %></span>
                </h3>

                <div class="space-y-3">
                    <% expense.splits.forEach(split => { %>
                        <div class="flex justify-between items-center p-4 rounded-2xl border <%= split.user._id.toString() === currentUser.toString() ? 'border-purple-200 bg-purple-50' : 'border-gray-50 bg-gray-50/50' %>">
                            <div class="flex items-center gap-3">
                                <div class="w-8 h-8 rounded-full bg-purple-200 flex items-center justify-center text-purple-700 font-bold text-xs">
                                    <%= split.user.username.charAt(0).toUpperCase() %>
                                </div>
                                <span class="font-semibold text-gray-700">
                                    <%= split.user.username %> 
                                    <% if(split.user._id.toString() === currentUser.toString()) { %>(You)<% } %>
                                </span>
                            </div>
                            <span class="font-bold text-gray-800">₹<%= split.amount.toFixed(2) %></span>
                        </div>
                    <% }) %>
                </div>

                <div class="mt-8 pt-6 border-t border-gray-50">
                    <div class="flex items-center gap-2 text-gray-400 text-sm">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                        <p>This expense was created via <%= expense.splitType %> splitting.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>